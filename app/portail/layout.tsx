import '../globals.css'

export const metadata = {
  title: 'Mon espace — FormaCRM',
  description: 'Espace personnel apprenant / formateur',
}

export default function PortalRootLayout({ children }: { children: React.ReactNode }) {
  return children
}
